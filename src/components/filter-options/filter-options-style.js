export let container = ({
    width: '100%',
    maxWidth: '20rem',
    display: 'flex',
    justifyContent: 'space-between',
});

export let title = ({
    margin: 0,
    padding: 0,
});

export let optionsContainer = ({
    display: 'flex',
    flexFlow: 'row wrap',
});

export let optionContainer = ({
    paddingLeft: '.5rem',
});
