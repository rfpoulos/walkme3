export let sort = ({
    display: 'flex',
    width: '100%',
    maxWidth: '20rem',
    justifyContent: 'space-between',
    paddingBottom: '1rem',
});

export let container = ({
    width: '100%',    
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    padding: '1rem',
});

export let input = ({
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    paddingBottom: '1rem',
});

export let walkContainer = ({
    display: 'flex',
    width: '100%',
    justifyContent: 'center',
    paddingBottom: '1rem',
});